plugins {
    id 'kotlin'
    id 'maven-publish'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId "com.skiiyis.study.launcher"
            artifactId "launcher"
            version "0.1.0-SNAPSHOT"

            from components.java
        }
    }
}